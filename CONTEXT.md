# GitFinder 领域上下文

## 产品目标

GitFinder 是面向本地开发工作的桌面工作台。长期目标是在用户明确添加的开发根目录内，接管 Finder 的高频开发管理工作：浏览和整理文件、识别项目、检查 Git、阅读文档、运行终端、查看任务与进度，并把高风险操作保持为可确认、可追溯、可恢复的动作。

首要管理范围是用户显式添加的本地开发根目录；macOS 上首要范围是 `/Volumes/project`。GitFinder 不接管系统目录，也不绕过系统权限、废纸篓/回收站或默认应用机制。

## 稳定概念

- **内容**：文件、目录、资料或其他本地条目，不要求存在 Git。
- **仓库**：带 Git 元数据的开发资产；仓库不等于项目。
- **项目**：用户显式设置的语义容器，由便携的 `.gitfinder/project.json` 标识，可关联零个、一个或多个仓库。项目身份不等于任务事实源。
- **仓库身份**：基于标准化远程地址，缺少远程时基于初始提交生成；不能随当前 HEAD 改变。
- **生命周期**：待整理、已规划、开发中、验证中、已部署、维护中、暂停、已冻结、已废弃或归档；与 Git 工作区状态分离。
- **事实源**：项目目录内的 CSV、Markdown、Git、测试和人工验收记录。
- **可视化投影**：GitFinder 显示的任务、进度和证据视图，不另造一套相互冲突的事实。

## 数据责任

- GitFinder 负责根目录、收藏、浏览历史、界面偏好、仓库注册表、分类/标签、Git/终端交互和文件操作日志。
- 结构化项目、任务、里程碑、进度与验收事实由 Local Project Manager 的项目事实源和功能牌协议负责；GitFinder 将其作为内嵌模块或连接器呈现，让用户仍在一个桌面入口中完成工作。
- 项目时间线由 Local Project Manager 的活动、证据、验收和自动化运行投影聚合；临时 Git 状态只作为当前上下文，不写入或伪装成持久化历史。
- GitFinder 不直接写 Local Project Manager 的业务 CSV；任务创建、状态、白名单计划字段和里程碑变更通过项目管理引擎自有的权威适配器完成，并携带修订号、结构化差异、幂等操作标识和人工确认结果。
- 在正式迁移决策前，不在 GitFinder 用户目录和项目目录之间复制两套任务状态。

## 安全与交互原则

1. 文件删除默认进入系统废纸篓；永久删除不是快捷操作。
2. 移动、批量修改、提交、推送和终端命令必须清楚显示作用范围。
3. AI 可以提出分类、任务和状态建议，但不得无确认改写项目事实或执行对外动作。
4. 路径移动后应重绑定稳定身份并保留分类、标签和证据关联。
5. 目录扫描与 Git 状态采集不得长期阻塞桌面交互。
6. 界面保持统一设计语言，交互分别符合 macOS Finder 与 Windows 文件资源管理器的熟悉性、可预测性和键盘可达性。
7. 新建普通文件必须使用不覆盖的独占创建；仅当文件仍为空且身份未变时才允许撤销，符号链接不得把写入引向受管根之外。
8. 从 Finder、桌面或其他应用拖入的内容一律按复制导入处理；确认前必须零写入展示计划，同名目标保留两份，应用时重新校验来源与目标，来源永不移动或改写。
9. 工作区标签顺序属于 GitFinder 的界面偏好，不改变项目、路径或任务事实；鼠标与键盘重排必须保持活动标签和各标签独立状态，并在重启后恢复。
10. 文件内容搜索只能由用户在全局搜索中显式切换启用；只读受管根内的有限白名单文本，必须可取消且限制文件数、单文件和总读取量，不得将文件内容或匹配片段写入持久化索引。
