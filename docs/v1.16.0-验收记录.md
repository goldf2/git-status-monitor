# GitFinder v1.16.0 验收记录

验收日期：2026-08-26
验收平台：macOS arm64
应用：`/Applications/GitFinder.app`
版本：`1.16.0`

## 自动化基线

- `npm test`：109 项通过，0 失败。
- JavaScript 语法检查：52 个文件通过。
- `npm audit --omit=dev`：0 个已知漏洞。
- 发布 zip 通过完整性检查；应用通过 ad-hoc `codesign --verify --deep --strict`。
- 当前仍无 Developer ID 签名和 Apple 公证，不得据此称为可对外无提示安装的稳定发行版。

## 文件夹与 Git 仓库语义视觉

在同一受管目录内放置普通文件夹和临时 Git 仓库，逐项检查目录树、卡片和列表视图：

- 普通文件夹使用低饱和蓝灰色。
- Git 仓库使用主题强调色和小型 `git` 标记。
- clean、dirty、ahead 和 behind 等 Git 状态色仍独立表达，没有被类型色覆盖。
- 三个视图的图标语义一致，实际窄窗口中未出现文字竖排或层叠。

## 工作区标签页重排

使用 Chromium DevTools Protocol 的可信输入事件验证真实渲染页：

1. 聚焦第一个标签页，发送 `Option+Shift+Right`；标签页向右移动一位。
2. 从末尾标签页的实际边界中心按下鼠标，连续经过 16 个中间点拖到首位；顺序随 drop 更新。
3. 两种操作后，活动标签 ID 始终不变，页面状态未被重建为新标签。
4. `workspaceTabSession` 中的顺序与 DOM 立即一致。
5. 完全退出并重启 GitFinder 后，DOM 顺序、活动标签和本机配置仍一致。

验收后已恢复原标签页顺序，并关闭两个 QA 标签页。

## 外部复制导入

生产 drop 处理器的端到端验收流程：

1. 通过 CDP `DOM.setFileInputFiles` 从真实本机路径创建 Chromium `File` 对象。
2. 将这些 `File` 对象经过实际 `drop` 处理器交给预加载层；`webUtils.getPathForFile` 正确恢复两个源路径。
3. 预览识别 2 个顶层项目、1 个同名冲突，目标规划为 `导入验收 副本.txt` 和 `样例项目`。
4. 预览显示期间，目标目录仍只有原始 `导入验收.txt`，操作历史未新增，证明预览零写入。
5. 确认后，副本文件与来源 SHA-256 一致；目录内 `README.md` 与来源 SHA-256 一致；原目标文件哈希未变。
6. 操作历史新增 `type: import`，记录 2 个目标和 1 个冲突。
7. 点击工具栏“撤销”后，两个导入目标移入系统废纸篓，`undoneAt` 已写入；原目标和两个来源哈希未变。

哈希基线：

- 来源 `导入验收.txt`：`1611789367779249240942f49400d61fde5e38089b1a39d5b18060783880a4fb`
- 原目标 `导入验收.txt`：`2922eccfdd7fd61768f78af4dda414532d6a70277722d3833de630b537a7af7f`
- 来源与导入后的 `README.md`：`226c534d654703935a1d6c93bcbacbcf00c17c07ac1ca61d40efafb173f3f22c`

### 验收边界

Computer Use 驱动在本次会话中无法把拖拽从 Finder 窗口交给另一个应用窗口，三次跨应用尝试都没有向 GitFinder 交付 drop 事件，且磁盘零变化。因此，本记录不把“鼠标从 Finder 窗口跨应用释放”本身标记为已验证；已验证的范围是真实本机 `File` 对象进入生产 drop 处理器后的全部路径提取、预览、确认、冲突命名和撤销链路。对外稳定发布前，仍应在一台真实用户设备上手工执行一次 Finder 跨应用释放。

## 验收后清理

- 两个 QA 标签页已关闭，活动工作区恢复到 `/Volumes/project/开发中`。
- `/Volumes/project/00-GitFinder-v115-QA` 已从项目根目录移出。
- 来源、目标、语义图标夹具与撤销产物集中保留在 `/tmp/GitFinder-v116-QA-evidence-20260826`，可恢复检查。
- 本次撤销生成的两个测试项目已从用户废纸篓移出，未留下同名测试垃圾。
