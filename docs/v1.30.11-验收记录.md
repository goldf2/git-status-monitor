# GitFinder v1.30.11 验收记录

日期：2026-08-28

## 本次范围

- Quick Look 新增 ZIP 目录预览，显示内部路径、压缩方式、压缩前后大小和加密标记。
- 只读取文件尾部与最多 8 MB 的中央目录；最多解析 10,000 个条目，并只向界面返回前 500 个。
- 不解压、不执行、不读取条目正文，也不写入或修改源归档。
- 损坏、分卷、Zip64、中央目录超限、条目数量超限、非法本地偏移或预览期间发生变化的归档会明确拒绝。
- 归档内路径在进入界面前规范化，并在渲染时转义，不把条目名称解释为 HTML。

## 自动化验证

- `npm test`：622/622 通过，176 个 JavaScript 文件通过语法检查。
- 新增覆盖：ZIP 中央目录解析、Unicode 路径、加密标记、文件夹统计、显示截断、损坏归档、分卷、Zip64、真实文件受限读取、服务路由和界面转义。
- `npm run pack`：macOS arm64 开发包构建完成；源码与产物门禁通过。
- 签名边界：开发包为 ad-hoc 签名，不具备正式分发、公证或自动升级发布资格。

## macOS 安装版桌面验收

- `/Applications/GitFinder.app` 已更新到 `1.30.11`，并从安装路径实际启动。
- 在安装版中打开真实构建归档 `GitFinder-1.30.11-arm64-mac.zip`，文件大小 `125944496` 字节。
- Quick Look 成功显示 `1201` 个条目、`319` 个文件夹、`882` 个文件、约 `120 MB` 压缩后体积和约 `287 MB` 原始体积。
- 界面明确显示只读安全说明及“仅显示前 500 个”的截断提示；列表可滚动，标题、统计、表头与条目没有文字重叠。
- 安装版与构建 App 的 `app.asar` SHA-256 一致：`cd63ffdceb5ce18bc032ed92690f8f9fe27f079b4fb6b0dfbbc7db2b32d395ae`。

## 构建产物

- ZIP：`dist/GitFinder-1.30.11-arm64-mac.zip`
- 大小：`125944496` 字节
- SHA-256：`b49c626e0d1eb436dc287c84df08c3034071ea14e86458f5d35191185722e1c4`
- 原 `1.30.10` App 可从 `/tmp/GitFinder-v13011-install-backup.zrPzsP/GitFinder.app` 恢复。

## 未完成与发布边界

- v1.30.11 没有生成新的 Windows 产物，也没有在 Windows 桌面验证 ZIP 预览；解析代码与自动化测试为跨平台，但不能据此描述为 Windows 已验收。
- Windows 当前可供虚拟机复测的仍是独立未签名预发布 `windows-v1.30.10-test1`；SmartScreen、用户原虚拟机以及 Windows 10/11 物理设备验收仍未完成。
- 本切片不包含 ZIP 解压或压缩。若后续实现，必须单独处理冲突、进度、取消、操作历史、磁盘空间和跨平台路径安全。
