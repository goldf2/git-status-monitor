# GitFinder v1.30.5 验收记录

日期：2026-08-28

## 功能范围

- 关系白板部署节点新增独立的环境、版本、分支、提交和声明状态字段，不再把版本上下文混写进备注。
- 手工“添加部署”和非模态详情检查器共用同一组受控字段；保存继续经过关系模型白名单、长度和敏感字段校验。
- 部署节点副标题、关键字搜索和“部署摘要”提示共同使用结构化版本上下文。
- Coolify 只读观测将 `git_branch` 和短 `git_commit_sha` 映射为部署分支与提交字段；备注只保留资源类型。
- 本切片没有增加部署、重启、SSH、Git 写操作或凭据持久化能力。

## 自动化验证

| 验证 | 结果 |
| --- | --- |
| 关系模型、白板控制器与 Coolify 定向测试 | 通过；覆盖结构化字段白名单、未知字段拒绝、手工创建、节点副标题、部署摘要和只读映射 |
| 完整 `npm run check` | 595/595 测试通过；170 个 JavaScript 文件语法检查通过 |
| 发布源码门禁 | `development` 通过 |
| macOS 产物门禁 | 通过；0 个问题、1 个预期的 ad-hoc 签名警告，`eligibleForDistribution: false` |

## 真实 macOS 桌面验收

- 使用独立 Electron `userData` 目录启动最终打包 App，不读取或修改现有关系白板事实。
- 载入“MES production → Con01”代表链路后，节点副标题完整提供 `production · v2.4.1 · release/2.4 · abcdef012345 · running`；右侧检查器分别显示名称、环境、版本、分支、提交、声明状态、事实来源、验证时间和证据摘要。
- 将版本从 `v2.4.1` 修改为 `v2.4.2` 后，“保存事实”变为可用；保存后节点立即更新并显示“关系事实已保存”。退出并用同一隔离目录重启后，`v2.4.2`、分支、提交、连线和白板模式均恢复。
- “添加节点 → 部署”真实表单依次显示部署名称、环境、版本、分支、提交和状态，并明确说明只保存本机配置、不会执行部署或 Git 写操作；点击取消后节点数和关系数保持不变。
- 隔离验收目录已移入 `/Users/tefulong/.Trash/GitFinder-v1305-ui-acceptance-20260828-1020`，可以恢复。

## 产物与本机安装

- 应用：`dist/GitFinder-darwin-arm64/GitFinder.app`，arm64，版本 `1.30.5`。
- 升级包：`dist/GitFinder-1.30.5-arm64-mac.zip`，SHA-256 `da888ab99fb15f79dd0e3cc443355292debcac398cb606ff3238d20a74a32d20`。
- `app.asar` SHA-256：`e45fb92576ea2f735d2df24ccb9e8f18373e8d432fe6a45bf5c6b20c692875b2`；安装前暂存副本、构建产物与安装后应用一致。
- `codesign --verify --deep --strict` 通过；签名为 ad-hoc，仅供本机开发使用，不具备正式分发资格。
- `/Applications/GitFinder.app` 已从 `1.30.4` 更新为 `1.30.5`，启动后实际窗口显示 `v1.30.5`，原目录、标签页与受管位置状态恢复。
- 旧 `1.30.4` 保存在 `/tmp/GitFinder-v1305-install-backup.VixOsh/GitFinder.app`，可恢复。

## 跨平台状态与未完成边界

- v1.30.5 的字段模型和渲染逻辑是跨平台代码，尚未生成或发布对应 Windows 产物，也未在真实 Windows 桌面验证白板表单、节点截断和重启恢复。
- v1.30.4 Windows x64 虚拟机启动修复版已由真实 `windows-latest` runner 通过无参数默认启动、安装、运行时和卸载验证，并发布为 `windows-v1.30.4-test1`；用户原虚拟机复测仍待反馈。
- 真实 Coolify 实例读取、差异确认、合并和令牌无落盘仍需用户主动授权；本轮只验证模拟只读 API，不把模拟结果描述为生产部署事实。
- Windows 代码签名、Windows 10/11 物理设备、UNC、文件占用、高 DPI、多显示器和 SmartScreen 人工验收仍未完成。
