# GitFinder v1.30.9 验收记录

日期：2026-08-28

## 本次范围

- 将关系白板原来的普通“分组节点”升级为布局层视觉分组。
- 成员关系只保存为当前白板 `boards[].placements[].groupId`，不写入 `relationships`，不允许悬空引用、非分组目标或嵌套分组。
- 多选检查器支持建立新分组、归入已有分组和移出分组；删除分组会安全解组并保留成员。
- 组框按成员位置动态计算，拖动组框和方向键移动会带动成员；框选与“适合内容”使用组框真实边界。
- JSON 导入在实体 ID 重映射后保留视觉分组成员关系。

## 自动化验证

- `npm test`：616/616 通过，174 个 JavaScript 文件通过语法检查。
- 新增覆盖：布局成员持久化、错误目标与嵌套拒绝、动态组框、整组移动、归组/解组、删除安全解组、单一撤销点及导入 ID 重映射。
- `npm run pack`：macOS arm64 开发包构建完成；源码与产物门禁通过。
- 签名边界：开发包为 ad-hoc 签名，不具备正式分发、公证或自动升级发布资格。

## 隔离 macOS 桌面验收

- 使用独立 Electron `userData` 目录启动最终打包 App，窗口显示 `v1.30.9`，未读取或修改安装版关系白板。
- 通过界面创建“验收服务器”“验收部署”和“验收分组”；空分组显示为组框，检查器明确显示“只属于当前白板布局，不参与部署或 Git 事实推理”，不显示事实核验控件。
- 隔离配置加入两个成员后重启，组框恢复为“2 个成员”，正确包围两张事实节点卡片。
- 拖动组框后，服务器、部署和分组布局坐标同步移动 94 px；退出时 JSON 已原子保存，第二次重启仍恢复“2 个成员”。
- 隔离验收目录已移入废纸篓：`/Users/tefulong/.Trash/GitFinder-v1309-ui-acceptance-20260828`。

## 构建与安装

- 应用：`dist/GitFinder-darwin-arm64/GitFinder.app`，arm64，版本 `1.30.9`。
- 升级包：`dist/GitFinder-1.30.9-arm64-mac.zip`，大小 `125938390` 字节。
- App `app.asar` SHA-256：`9ed857b1035811aad8c626b0c8698cac510c503dd06bcaf586885a06aa9e8e38`。
- ZIP SHA-256：`38bfcb84fa949618dc41ec1cda7d9479510113e1768adaa7a4dddedec75b0535`。
- `/Applications/GitFinder.app` 已更新为 `1.30.9`，安装后 App `app.asar` 与构建产物哈希一致；实际启动窗口显示 `v1.30.9`，原有标签、当前目录、受管位置、项目和收藏恢复。
- 原 `1.30.8` App 可从 `/tmp/GitFinder-v1309-install-backup.dXAedW/GitFinder.app` 恢复。

## 未完成与发布边界

- v1.30.9 未生成新的 Windows 产物，也未在真实 Windows 桌面验证视觉分组，不能描述为 Windows 已发布或已验收。
- 分组折叠、嵌套分组和自动布局未纳入本切片；嵌套分组当前明确拒绝。
- 本次只做白板布局整理，不新增部署执行、服务器连接、Git 写操作或关系事实推断。
