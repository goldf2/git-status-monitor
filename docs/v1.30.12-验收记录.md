# GitFinder v1.30.12 验收记录

日期：2026-08-28

## 本次范围

- Quick Look 页眉增加上一项、下一项与当前位置计数，按钮和无修饰键方向键沿当前目录的完整显示顺序连续浏览。
- 切换预览时同步目录单选、标题、路径、计数和安全预览内容；关闭后把键盘焦点交还给当前项目。
- 统一窗口关闭策略：有明确关闭或取消控件的模态窗口不再响应背景遮罩点击，只接受显式控件或原有 Escape 路径。
- Quick Look、前往文件夹、名称输入、文件标签、内容筛选、智能集合、性能诊断、批量重命名、项目设置、导入审查、任务窗口与关系白板对话框均纳入同一规则。
- 下拉菜单、工具栏菜单、筛选菜单和右键菜单仍保持点击外部收起，不改变临时浮层习惯。

## 自动化验证

- `npm run check`：通过，630 项测试全部通过。
- JavaScript 语法检查：通过，共检查 178 个文件。
- 新增窗口关闭策略回归，禁止重新引入背景遮罩关闭，并确认工具栏菜单和文件右键菜单仍保留外部点击收起。
- 新增名称对话框和 Quick Look 行为回归：背景点击保持打开，显式取消或关闭仍正常结束。
- Quick Look 连续浏览回归覆盖当前完整显示顺序、目录单选同步、位置计数、边界按钮、方向键入口、关闭焦点恢复和预览令牌撤销。

## macOS 安装版桌面验收

- `/Applications/GitFinder.app` 已更新到 `1.30.12`，并从安装路径实际启动；页眉版本显示 `v1.30.12`。
- 连续 Quick Look 在真实构建归档上验收：从 `GitFinder-1.30.11-arm64-mac.zip` 的 `35 / 51` 切换到 `GitFinder-1.30.12-arm64-mac.zip` 的 `36 / 51`，目录选择、标题、路径和 ZIP 只读摘要同步更新；方向键可继续切换。
- 最终安装包中打开“前往文件夹”，点击弹窗外背景后窗口保持打开；点击右上关闭按钮可以正常关闭，重新打开后 Escape 也可以关闭。
- 最终安装版与构建 App 的 `app.asar` SHA-256 一致：`3872b98b706977c0e60d51a2357c91b733175b20eca63108c4371368ecbf3dbf`。

## 构建产物

- ZIP：`dist/GitFinder-1.30.12-arm64-mac.zip`
- 大小：`125947559` 字节
- SHA-256：`881ad07fd68793276caf8151b78594509e04975fbbc92e7fc1a1ad1f5c87060b`
- 覆盖安装前的旧 `1.30.12` App 可从 `/tmp/GitFinder-v13012-modal-install-backup.2j2Sec/GitFinder.app` 恢复。
- 本次 macOS 构建为 ad-hoc development 包，不具备正式签名、公证或稳定分发资格。

## 未完成与发布边界

- v1.30.12 没有生成新的 Windows 产物，也没有在 Windows 桌面复验连续 Quick Look 或窗口背景点击；跨平台源码与自动化通过不能替代真实 Windows 验收。
- Windows 当前可供虚拟机复测的仍是独立未签名预发布 `windows-v1.30.10-test1`；用户原虚拟机闪退复测、SmartScreen、Windows 10/11 物理设备、高 DPI、UNC 和文件占用验收仍未完成。
- 本切片不改变文件写入、Git、部署或 Local Project Manager 数据边界。
