# GitFinder v1.30.6 验收记录

日期：2026-08-28

## 功能范围

- 选择服务器节点后，详情检查器从既有 `runs_on`、`source_of` 和 `contains` 关系反向派生关联部署、仓库与项目。
- 每个关联部署显示环境、版本、分支、提交和声明状态；无项目或仓库关系时明确标记缺失上下文。
- “定位”仅对已放入当前白板的部署可用；会保留节点显示模式，清除遮挡筛选、切回完整事实并居中选择目标。
- 清单是本机关系事实的只读视图；本切片没有增加 SSH、部署、重启、实时状态读取、Git 写操作或凭据持久化能力。

## 自动化验证

| 验证 | 结果 |
| --- | --- |
| 关系白板控制器定向测试 | 28/28 通过；覆盖服务器反向聚合、结构化版本上下文、不可定位状态，以及清除筛选与摘要后的定位 |
| 完整 `npm run check` | 597/597 测试通过；170 个 JavaScript 文件语法检查通过 |
| 发布源码门禁 | `development` 通过 |
| macOS 产物门禁 | 通过；0 个问题、1 个预期的 ad-hoc 签名警告，`eligibleForDistribution: false` |

## 真实 macOS 桌面验收

- 使用独立 Electron `userData` 目录启动最终打包 App，不读取或修改现有关系白板事实。
- 验收事实链为 `MES Project → mes-lite → MES production → Con01`，初始显示为不改写事实的“部署摘要”。
- 选择 Con01 后，右侧“关联部署 · 1”同时显示部署、项目、仓库，以及 `production · v2.4.2 · release/2.4 · abcdef012345 · running`；说明文字明确表示不会连接服务器或检查实时运行状态。
- 点击“定位”后，白板从部署摘要切换为 4 个节点、3 条关系的完整事实视图，MES production 被选中并居中，画布公告为“已在当前白板定位 MES production”。
- 视觉检查未发现关联清单文字层叠；超长单行按详情面板宽度安全截断，完整内容仍在可访问树和悬停标题中提供。
- 隔离验收目录已移入 `/Users/tefulong/.Trash/GitFinder-v1306-ui-acceptance-20260828-1038`，可以恢复。

## 产物与本机安装

- 应用：`dist/GitFinder-darwin-arm64/GitFinder.app`，arm64，版本 `1.30.6`。
- 升级包：`dist/GitFinder-1.30.6-arm64-mac.zip`，SHA-256 `a03b266ca0a3891df8f0df1fb15be4f6a17eb9a9a1e8995b7a27dc01ed42be19`。
- `app.asar` SHA-256：`0ef81f4eb08b164119acd98e1ef096b16d732296784ba10cb0357c56de64261f`；构建产物、安装暂存副本与安装后应用一致。
- `codesign --verify --deep --strict` 通过；签名为 ad-hoc，仅供本机开发使用，不具备正式分发资格。
- `/Applications/GitFinder.app` 已从 `1.30.5` 更新为 `1.30.6`；实际窗口显示 `v1.30.6`，原目录、标签页、受管位置和收藏状态恢复。
- 旧 `1.30.5` 保存在 `/tmp/GitFinder-v1306-install-backup.d9Sscc/GitFinder.app`，可恢复。

## 跨平台状态与未完成边界

- v1.30.6 的服务器聚合与定位是跨平台渲染层代码，尚未生成或发布对应 Windows 产物，也未在真实 Windows 桌面验证此白板交互。
- v1.30.4 Windows x64 虚拟机启动修复版已由真实 `windows-latest` runner 通过无参数默认启动、安装、运行时和卸载验证，并发布为 `windows-v1.30.4-test1`；用户原虚拟机复测仍待反馈。
- 真实 Coolify 实例读取、差异确认、合并和令牌无落盘仍需用户主动授权；本轮只验证本机事实清单，不把它描述为生产运行状态。
- Windows 代码签名、Windows 10/11 物理设备、UNC、文件占用、高 DPI、多显示器和 SmartScreen 人工验收仍未完成。
