# GitFinder v1.30.7 验收记录

日期：2026-08-28

## 功能范围

- 工作区标签总宽度超过可见区域时，标签栏显示左右浏览按钮；到达边界后对应按钮禁用。
- 活动标签被横向浏览到屏幕外时，对应方向按钮使用强调色提示；点击该方向时优先一次找回活动标签，活动标签仍可见时才按约 72% 可见宽度继续浏览。
- 标签切换、恢复、重排或重新渲染后，活动标签自动保持在可见区域。
- 标签原生悬停提示只显示完整标签名称，不再显示遮挡工具栏的绝对路径；完整路径仍保留在标签的无障碍描述中。

## 自动化验证

| 验证 | 结果 |
| --- | --- |
| 标签溢出控制器定向测试 | 4/4 通过；覆盖屏幕外活动标签方向提示、一键找回、普通分页浏览、渲染后可见性和页面接线 |
| 完整 `npm run check` | 601/601 测试通过；172 个 JavaScript 文件语法检查通过 |
| 发布源码门禁 | `development` 通过 |
| macOS 产物门禁 | 通过；仅有预期的 ad-hoc 签名警告，`eligibleForDistribution: false` |

## 真实 macOS 桌面验收

- 使用独立 Electron `userData` 目录启动最终打包 App，不修改当前安装版的标签会话和应用配置。
- 将窗口缩至应用允许的最小宽度并打开 9 个标签，真实形成横向溢出；左右按钮按当前位置正确启用和禁用。
- 激活最右侧“开发中”标签后向左浏览，使活动标签退出可见区域；右侧按钮显示强调色方向提示。
- 点击强调后的右侧按钮一次即返回“开发中”活动标签，标签内容和当前目录未改变。
- 无障碍树中标签 `Help` 只包含完整标签名，`Description` 仍包含标签名和绝对路径，证明视觉提示缩短后没有丢失路径语义。
- 隔离验收目录已移入 `/Users/tefulong/.Trash/GitFinder-v1307-ui-acceptance-20260828-1130`，可以恢复。

## 产物

- 应用：`dist/GitFinder-darwin-arm64/GitFinder.app`，arm64，版本 `1.30.7`。
- 升级包：`dist/GitFinder-1.30.7-arm64-mac.zip`，SHA-256 `f844b22929dace2e602fc44757a3a68db72d438358fbd8ffe15bd373702ade4b`。
- `app.asar` SHA-256：`4524eca41f2a5c9dca5eaadd50ddfd1bd53c85fc0d28cbc057838e18ce616427`；构建产物、安装暂存副本与安装后应用一致。
- `codesign --verify --deep --strict` 通过；签名为 ad-hoc，仅供本机开发使用，不具备正式分发资格。
- `/Applications/GitFinder.app` 已从 `1.30.6` 更新为 `1.30.7`；实际窗口显示 `v1.30.7`，原目录、标签页、受管位置和收藏状态恢复。
- 旧 `1.30.6` 保存在 `/tmp/GitFinder-v1307-install-backup.YKGZWB/GitFinder.app`，可恢复。

## 跨平台状态与未完成边界

- 本切片是跨平台渲染层交互，但 v1.30.7 尚未生成 Windows 产物，也未在真实 Windows 桌面验证标签溢出行为。
- v1.30.4 Windows x64 虚拟机启动修复测试版已由真实 `windows-latest` runner 通过默认启动、安装、运行时和卸载验证；用户原虚拟机复测仍待反馈。
- Windows 代码签名、Windows 10/11 物理设备、UNC、文件占用、高 DPI、多显示器和 SmartScreen 人工验收仍未完成。
